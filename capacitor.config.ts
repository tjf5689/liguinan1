import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.example.znldk',
  appName: '正能量打卡',
  webDir: 'dist',
  server: { androidScheme: 'https' }
};
export default config;